server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # สำคัญมาก — ทำให้ React Router ทำงานได้ถูกต้อง
        # ถ้าไม่มีบรรทัดนี้ การ refresh หน้าจะได้ 404
        try_files $uri $uri/ /index.html;
    }
}
